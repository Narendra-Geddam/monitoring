# Development/Testing Configuration
# Low resource requirements, temporary storage
# Perfect for quick testing and development

prometheus:
  enabled: true
  prometheusSpec:
    retention: 2d
    retentionSize: "5GiB"
    resources:
      requests:
        cpu: 100m
        memory: 512Mi
      limits:
        cpu: 250m
        memory: 1Gi
    storageSpec:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false

grafana:
  enabled: true
  adminPassword: admin123
  persistence:
    enabled: true
    size: 3Gi
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
    limits:
      cpu: 200m
      memory: 256Mi

alertmanager:
  enabled: true
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 3Gi

nodeExporter:
  enabled: true

kubeStateMetrics:
  enabled: true

prometheusOperator:
  enabled: true
  manageCrds: true

defaultRules:
  create: true

# Note: For EmptyDir (non-persistent) testing, modify storageSpec sections:
# emptyDir:
#   sizeLimit: 10Gi
# This will lose data on pod restart but is useful for quick tests
